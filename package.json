{
  "name": "indirect-lighting-simulator",
  "version": "2.0.1",
  "description": "間接照明模擬器",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000 --host",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.0",
    "vite": "^5.4.0"
  }
}
